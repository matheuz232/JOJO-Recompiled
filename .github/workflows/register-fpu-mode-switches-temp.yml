name: register-fpu-mode-switches-temp

on:
  push:
    branches:
      - feature/sh4-fpu-mode-switches
    paths:
      - .github/workflows/register-fpu-mode-switches-temp.yml

permissions:
  contents: write

jobs:
  register-test:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feature/sh4-fpu-mode-switches
          fetch-depth: 0
      - name: Register test target
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          path = Path("CMakeLists.txt")
          text = path.read_text(encoding="utf-8")
          marker = "add_test(NAME jojo_sh4_flds_fsts_tests COMMAND jojo_sh4_flds_fsts_tests)"
          if text.count(marker) != 1:
              raise SystemExit("FLDS/FSTS CMake marker not found exactly once")
          addition = (
              marker + "\n"
              "add_executable(jojo_sh4_fpu_mode_switch_tests tests/test_sh4_fpu_mode_switches.cpp)\n"
              "target_link_libraries(jojo_sh4_fpu_mode_switch_tests PRIVATE jojo_core)\n"
              "add_test(NAME jojo_sh4_fpu_mode_switch_tests COMMAND jojo_sh4_fpu_mode_switch_tests)"
          )
          path.write_text(text.replace(marker, addition, 1), encoding="utf-8")
          PY
          git diff --check
      - name: Commit test registration
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add CMakeLists.txt
          git commit -m "test: register SH-4 FPU mode switch coverage"
          git push origin HEAD:feature/sh4-fpu-mode-switches
