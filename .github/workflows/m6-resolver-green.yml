name: m6-resolver-green

on:
  push:
    branches: [feature/m6-mod-runtime]
    paths:
      - 'src/core/mod_runtime.h'
      - 'src/core/mod_resolver.cpp'
      - 'tests/test_mod_runtime.cpp'
      - '.github/workflows/m6-resolver-green.yml'

jobs:
  resolver-green:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build resolver test
        run: c++ -std=c++20 -Wall -Wextra -Wpedantic -Isrc tests/test_mod_runtime.cpp src/core/mod_runtime.cpp src/core/mod_resolver.cpp src/core/semver.cpp -o /tmp/test_mod_runtime
      - name: Run resolver test
        run: /tmp/test_mod_runtime
