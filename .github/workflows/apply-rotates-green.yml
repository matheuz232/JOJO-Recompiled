name: Apply SH-4 rotate GREEN

on:
  push:
    branches:
      - feature/sh4-rotates

permissions:
  contents: write

jobs:
  apply:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Apply deterministic rotate edits
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/apply_rotates_green.py
          git diff --check
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: implement SH-4 rotate instructions"
          git push origin "HEAD:${GITHUB_REF_NAME}"
